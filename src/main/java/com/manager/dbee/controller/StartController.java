package com.manager.dbee.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

@Controller
public class StartController {

	/*
	 * @GetMapping("/") public String startPage() { return "login"; //
	 * resources/templates/login.html 을 보여줌 }
	 * 
	 * @GetMapping("login") Public String login(@RequestParam("id") String
	 * id, @RequestParam("pwd") String password) {
	 * 
	 * }
	 */
}
